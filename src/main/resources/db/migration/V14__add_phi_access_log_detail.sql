-- =========================================================
-- V14__add_phi_access_log_detail.sql
-- 원내 기록은 원내에만 남긴다.
--
-- 간호기록·활력징후·주의사항의 감사 기록과 간호기록 수정 전후 내용이
-- 그동안 업무 쪽 audit_log 에 쌓였다. 두 서버로 갈라지면 원내에는 그 테이블이 없고,
-- 무엇보다 **간호기록 수정 전 내용은 그 자체가 진료정보다.** 업무 쪽에 두면
-- 나눈 의미가 사라진다.
--
-- 그래서 원내 자체 접근 기록에 무엇이 바뀌었는지 담을 자리를 만든다.
-- =========================================================

ALTER TABLE phi_access_log ADD COLUMN detail JSONB;

COMMENT ON COLUMN phi_access_log.detail IS
'간호기록 수정처럼 무엇이 바뀌었는지 남겨야 하는 경우의 전후 내용. 별도 이력 테이블을 두지 않는다';
