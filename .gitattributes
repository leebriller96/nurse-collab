# 기본은 LF. Windows 로컬에서만 CRLF 로 체크아웃된다.
* text=auto eol=lf

# CRLF 가 섞이면 리눅스/CI 에서 실행되지 않는다.
gradlew    text eol=lf
*.sh       text eol=lf
*.bat      text eol=crlf

*.jar      binary
*.png      binary
*.jpg      binary
